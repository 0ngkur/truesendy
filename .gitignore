node_modules/
.env
.env.local
.env.*.local
.DS_Store

# ── Runtime data — never commit (user PII, bcrypt hashes, plaintext OTPs, keys) ──
db/*.json
uploads/
dist/
.pkg-cache/
*.exe
*.log

# ── Local dev launcher (contains dev secrets) ──
start-server.bat

# ── Local tooling artifacts (session memory, planning, crash dumps) — never push ──
.planning/
.remember/
.claude/
.playwright-mcp/
*.stackdump
REVIEW.md
AUDIT_REPORT.md
plans/

# ── Electron desktop app — build artifacts (huge + regenerable), never commit ──
.electron-cache/
desktop/dist/
truesendy-debug.log

# ── Test/scratch files — never commit (local dev only) ──
test-api.js
test-dns.js
test-smtp.js
test_e2e.js
test_upload.js
test_emails.csv
test_emails.txt
test*.js
test*.csv
test*.txt
vps-*.png

db/greylist.json
start.bat
